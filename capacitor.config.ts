import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.etm.v333',
  appName: 'ETM Module v3.33',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;